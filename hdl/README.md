# HDL Source

Place reviewable Verilog, SystemVerilog, VHDL, and testbench source here.

Each runnable target must be declared by a manifest before an adapter consumes it. Generated simulator source, executables, traces, and reports belong below `out/`, never in this directory.
